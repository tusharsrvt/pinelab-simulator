server.port=8080
spring.application.name=pinelab-cbs-simulator

# Simulator config file location
simulator.config.file=simulator-config.yaml
# Enable auto-reload when config file changes (default: true)
simulator.config.auto-reload=true

# Logging
logging.level.com.pinelab.simulator=INFO
logging.level.org.springframework.web=INFO